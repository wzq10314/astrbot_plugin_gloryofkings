# GloryOfKings-Plugin

<div align="center">

![动态访问量](https://count.kjchmc.cn/get/@GloryOfKings-Plugin?theme=rule34)

Yunzai-Bot V3 的王者荣耀插件：战绩查询、赛季表现、皮肤墙、战绩推送、日周月报、营地观战、营地好友消息、跨机器人营地ID共享。

**QQ 交流群：972915804**

</div>

> [!NOTE]
> 本仓库是 [@Tloml-Starry/GloryOfKings-Plugin](https://gitee.com/Tloml-Starry/GloryOfKings-Plugin) 的 Fork，新增皮肤墙、常用英雄、称号墙、趋势图等功能，补齐了原插件只有配置项没实现的**战绩推送 / 上下线提醒**；营地接口参考 [@KimigaiiWuyi/WzryUID](https://github.com/KimigaiiWuyi/WzryUID)，原插件版权与协议归原作者所有。
> 数据全部来自腾讯《王者荣耀》官方（游戏客户端、王者营地 App 及接口），版权归腾讯所有，这里只是把公开接口做成便于查询的形式，**与本人无关**；数据可能有延迟、缺漏或偏差，请以游戏内和营地 App 为准。**仅供参考、请勿商用**，使用本插件产生的后果由使用者自行承担。

## 安装

在**云崽根目录**执行，三个源任选一个（内容完全一样，推荐国内的 gitcode 或 gitee）

```bash
git clone --depth=1 https://gitcode.com/ccxhan/GloryOfKings-Plugin.git ./plugins/GloryOfKings-Plugin/
```

```bash
git clone --depth=1 https://gitee.com/longhengmu/GloryOfKings-Plugin.git ./plugins/GloryOfKings-Plugin/
```

```bash
git clone --depth=1 https://github.com/cchanlan/GloryOfKings-Plugin.git ./plugins/GloryOfKings-Plugin/
```

```bash
cd ./plugins/GloryOfKings-Plugin && pnpm install
```
然后重启云崽即可。
## 指令

### 账号管理

| 指令 | 说明 |
| --- | --- |
| `#绑定营地 <营地ID>` / `#切换营地 <序号>` / `#删除营地 <序号>` | 绑定 / 切换 / 删除营地账号 |
| `#营地ID` `#王者ID` `#我的王者ID` / `#获取营地ID` `#怎么看营地ID` | 已绑账号列表 / 营地ID 获取教程图 |
| `#营地wx全局登录` / `#营地QQ全局登录` | 扫码登录：拿登录态 + 自动绑定这个营地号，登录后才能用 `#营地观战` |

登录态「**用则续命、闲置才死**」，插件每天自动保活，一般不用管；QQ 号失效会自动重登，微信号只能重新扫码。想手动催一次：`#营地续期`（请主人发）。

### 战绩与表现

| 指令 | 说明 |
| --- | --- |
| `#查询战绩` `#排位战绩` `#巅峰战绩` `#查询N战绩` | 最近 30 场（`N` 为第几个绑定号）；点开某场看双方十人评分 / 经济 / 出装 / 参团率 / 五维评级 |
| `#查战绩 <英雄名>` | 如 `#查敖隐战绩`，英雄名放前后都行 |
| `#王者主页` `#王者卡片` `#王者信息` / `#全部王者主页` `#全部主页` | 段位、巅峰分、战力、胜率、皮肤数一张图 / 名下所有账号 |
| `#排位表现` / `#巅峰表现` / `#赛季表现` | 段位积分、分路、常用英雄、雷达图、上分趋势；加 `s40` 看指定赛季，`#赛季表现` 是排位 + 巅峰合并成一张图 |
| `#全部排位表现` `#全部赛季表现` / `#全部巅峰表现` | 历届赛季汇总，默认最近 3 个 |
| `#王者对比` `#对比` / `#王者日报` `#战绩日报` `#王者周报` `#王者月报` | 两个人的数据对照 / 日周月战绩总结图 |
| `#巅峰趋势` `#上分趋势` `#巅峰曲线` / `#段位趋势` `#排位趋势` `#段位曲线` | 巅峰分折线（默认近 14 天）/ 段位升降阶梯图 |
| `#排位排名` / `#巅峰排名` / `#排位总排名` / `#巅峰总排名` | 本群成员排名 / 全部已绑用户排名，加「刷新」强制重算 |
| `#导出战绩` `#战绩导出` | 把本地战绩归档导成 CSV 发出来，拿去 Excel 自己拉透视表 |

### 英雄与皮肤

| 指令 | 说明 |
| --- | --- |
| `#常用英雄` `#英雄战力榜` / `#我的英雄` | 本赛季场次 / 胜率 / 战力 / 称号；全部英雄的历史最高战力 / 称号 / 场次 / 胜率 |
| `#英雄详情 <英雄名>` / `#查战力 <英雄名>` | 生涯场次胜率、荣耀战力曲线、表现五维、最近对局 / 指定英雄战力 |
| `#英雄梯度` `#梯度` `#强度` / `#英雄攻略 <英雄名>` `#出装` `#克制` `#铭文` | 前者：T0~T3 实时强度，可按段位 / 分路筛；后者：出装、铭文、核心装备胜率、克制关系 |
| `#称号墙` `#荣耀称号` `#我的称号` / `#皮肤墙` `#全部皮肤` | 荣耀称号一览 / 皮肤按价值倒序出图，可 `#皮肤墙 20` 限制数量 |
| `#缺皮肤` `#皮肤缺失` `#缺哪些皮肤` / `#查皮肤 <英雄名>` / `#皮肤上新` `#新皮肤` `#皮肤日历` | 还差哪些皮肤 / 英雄皮肤信息 / 即将上线与最近上线的皮肤 |

### 营地观战

> `#营地观战` 可简写成 `#观战`，下面所有子指令都能换着发（如 `#观战 在播`）。

| 指令 | 说明 |
| --- | --- |
| `#营地观战` / `#营地观战 列表` | 看**自己**营地好友里谁正在对局（含段位、开局多久，图上有编号） |
| `#营地观战 <编号>` | 开一路直播，群里发个链接，群友点开就能看，手机电脑都行 |
| `#营地观战 在播` `#正在播` `#直播间` `#rooms` | 看现在有哪几路直播在跑 |
| `#营地观战 停` `#停止` `#关` `#关闭` `#stop` | 停掉**自己开的**直播间，别人在看的不受影响 |
| `#营地观战 停 <编号>` / `#营地观战 停 全部` `#全停` `#关闭全部` `#全关` | 停指定的一路（编号看 `#营地观战 在播`，不是名单图上的编号）/ 停掉全部 |
| `#营地开播` | 开「本群最近提示的那一场」——订阅上下线提醒的人开局满几分钟后群里会收到开播提示 |

看的是**发起人自己的营地好友**，要先 `#营地QQ全局登录` 或 `#营地wx全局登录` 扫码（登录态记在扫码人名下）；一个对局一路、每路独立网址和独立聊天室，**一路要占一个账号**；直播间里群友能一起聊、发的话会飘成弹幕，记录只在内存留最近 100 条（启动加 `GOK_CHAT=0` 可关）；播放页有「开始录像」按钮，默认不录。

观战要**另跑一个后端进程**（取流和录像），机器上还得有 ffmpeg。装完把**直播间对外地址**填成外网能访问的（域名或公网 IP）并放行 `8899` 端口，不然群友点开是白屏：

```
#营地观战接入 <地址> <令牌>      # 地址和令牌进群 972915804 找主人要
#营地观战部署 / #营地观战服务     # 更新代码 / 看进程、ffmpeg、对外地址配好没
```

### 营地消息

营地好友发来的消息会**转到你的 QQ 私信**，你可以直接回（仅私聊）。

| 指令 | 说明 |
| --- | --- |
| **引用那条推送回一句** / `#营地回复 <营地号> <内容>` | 直接回复对方 / 不用引用也行，指定用哪个号回 |
| `#营地好友` / `#营地私聊 <编号> <内容>` | 看在游戏里的好友（图上有编号，10 分钟内有效）/ 按编号给 TA 发消息 |
| `#营地消息` | 看服务状态、哪些号在线、还有多少条待处理 |
| `#营地消息开` `#营地消息开启` `#营地消息打开` `#营地消息收` | 让自己名下的营地号开始收消息（扫过的号默认只用来查询，要收得自己开） |
| `#营地消息关` `#营地消息关闭` `#营地消息停止` `#营地消息不收` | 让自己名下的营地号不再收消息 |
| `#营地消息接入 <地址> <令牌>` | 一步装好服务，地址令牌进群 972915804 找主人要（和观战共用） |
| `#营地消息部署` / `#营地消息服务` / `#营地消息同步` `#营地消息重连` | 更新代码 / 看状态 / 按开关重连账号 |

每个营地号的收发**只推给它的归属人**（扫码登录时记下，可在锅巴「账号列表」改「归属 QQ」，没有归属人的号一律不推）；只推真人好友消息，直播流推送和公众号一律不推。

⚠️ **收消息是一份独立名单**，跟查询/推送轮询用的账号池是两回事 —— 扫进来的全局账号默认**只拿来轮询查数据**，不挂长连接收消息。要收的号发一句 `#营地消息开` 加进来，也可以在锅巴侧边栏「营地消息」页面点着开关，改完几秒生效、不用重启。

### 推送订阅与报告

```
#开启战绩推送 / #关闭战绩推送          打完一局自动发全场详情图
#开启上下线提醒 / #关闭上下线提醒       对方上号 / 下号提醒
#开启在线状态 / #关闭在线状态          是否采集在线状态（关了就退出 #谁在打游戏 名单）
#战绩推送状态                        看自己订阅了什么
#开启皮肤上新推送 / #关闭皮肤上新推送    新皮肤上线播报（限群管理）
#谁在打游戏 #谁在打王者 #谁在玩王者 #谁在上号 #谁在排位 #王者在线 #王者在线列表 #王者在线状态 #在线列表

#开启日报推送 / #关闭日报推送          每晚自动在本群发当日总结，没打就不发（周报、月报同理）
#群日报 #群周报 #群月报               全群汇总（限群管理），别名 #王者群日报 等
#开启群日报推送 / #关闭群日报推送      群订阅（周报、月报同理）
#群报状态 / #群报推送状态
```

战绩推送和上下线提醒两个开关互相独立，只在打完新的一局后触发，不会重复推；报告读的是本地战绩归档，不额外刷营地接口。

### 营地ID 共享（可选的跨机器人功能）

你在 A 群的机器人上绑过营地ID，被拉到 B 群（另一个 bot 主人搭的实例）时，不用重新绑定就能直接查战绩。**默认不共享**，要自己发指令开：

```
#开启营地ID共享   #开启营地共享 #打开营地共享    把营地ID传上去
#关闭营地ID共享   #取消营地共享                取消共享，之前传上去的也会删掉
#营地ID共享状态   #营地共享状态                看看自己开没开、共享了哪些号
#同步营地ID共享                               立刻推一次，之后改绑定不会自动跟着走
```

上传的是「QQ 号的加盐哈希 → 营地ID」，**不上传登录态、不上传战绩，一个字的凭证都不上传**。共享库由主人统一提供，**地址和令牌进群 `972915804` 找主人要**，三步接上：`#营地共享库地址 <地址>` → `#营地共享库令牌 <令牌>` → `#接入营地共享库`；本机要有可用的全局账号才查得动，共享过来的号在本机查过一次后就会记下来（**按需落地**，没人查过不会主动同步）。

### 主人指令（仅主人）

```
#王者设置 #王者用户统计 #清理失效营地账号 #清空王者战绩推送 #营地续期
#王者拉黑 @某人 / [QQ号]   #王者取消拉黑   #王者黑名单
#王者数据备份 #王者备份列表   #王者缓存状态 #缓存信息 / #清理王者缓存
#王者帮助 / #王者更新 #王者强制更新 / #王者更新日志
#营地共享库 #营地共享库发令牌 <备注> #营地共享库接入方 #营地共享库吊销 <序号> #营地共享库同步
#营地共享库查 <QQ> #营地共享库地址 <地址> #营地共享库令牌 <令牌> #营地共享库管理密钥 <密钥>
#营地共享库提醒 #接入营地共享库 #关闭营地共享库
```

拉黑后，名单里的人发任何指令都没回应，之前订阅的推送也停掉；默认还跟随机器人自身的 `config/config/other.yaml`（`blackUser` / `blackQQ`）—— 那份名单挡不住插件按时间主动发的推送，靠它一并停掉，不想跟随可在 `#王者设置` 里关掉。

## 致谢与协议

- [@Tloml-Starry/GloryOfKings-Plugin](https://gitee.com/Tloml-Starry/GloryOfKings-Plugin) —— 原插件；
-  [@KimigaiiWuyi/WzryUID](https://github.com/KimigaiiWuyi/WzryUID) —— 营地接口调用逻辑参考；数据来自王者营地与王者荣耀官网资料库

  [MIT](LICENSE) © 2026 cchanlan · 本仓库为 Fork，原插件版权归原作者所有，在此一并致谢。觉得有用点个 Star ⭐
